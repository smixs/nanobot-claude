# Optional environment overrides for the nanobot systemd unit.
# The shim path is local (127.0.0.1:8787) so no proxy is required.
# Kept as a seam for future knobs (extra channels, custom env).

# Example:
# TELEGRAM_BOT_TOKEN=123456:ABC-DEF...
