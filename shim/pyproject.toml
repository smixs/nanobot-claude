[project]
name = "claude-code-shim"
version = "0.1.0"
description = "OpenAI-compat HTTP shim that delegates to the claude CLI"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115",
  "uvicorn[standard]>=0.30",
]

[tool.uv]
package = false
